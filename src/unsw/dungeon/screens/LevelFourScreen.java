package unsw.dungeon.screens;

import java.io.IOException;
import javafx.stage.Stage;

public class LevelFourScreen extends Screen {

	public LevelFourScreen(Stage stage, String filename) throws IOException {
        super(stage, filename, "Getting to an exit and (kill all enemies or get all treasure)");
    }
}